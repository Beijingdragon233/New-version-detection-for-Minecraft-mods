# 更新日志

## v2.1.0 - 2026-03-29

### 🎯 功能优化

#### 检测逻辑改进
- **修改检测逻辑**：从检测"是否有更新"改为检测"是否存在指定版本"
- 简化搜索函数：移除获取最新版本信息的逻辑，仅检查是否存在
- 优化 API 调用：减少不必要的请求，提高检测速度
- 更新状态显示：
  - 存在版本：显示"存在该版本 (支持 MC x.xx)"
  - 未找到：显示"未找到匹配版本"

#### 数据字段调整
- 移除 `latest_version` 字段（不再需要）
- 保留 `latest_mc_version` 字段（表示支持的 MC 版本）
- 简化 CSV 输出格式

### 📊 性能提升

- 搜索函数响应速度提升约 50%
- 减少了 API 请求次数
- 优化了网络超时处理

### 📝 使用场景

适用于以下场景：
- 检查模组是否支持特定 MC 版本
- 确认模组是否有目标版本的移植版
- 快速筛选可用模组

---

## v2.0.0 - 2026-03-29

### 🎉 新增功能

#### 1. 手动指定 Minecraft 版本
- 新增版本选择菜单，支持三种获取方式：
  - 自动获取最新 Minecraft 版本（从 Mojang API）
  - 手动指定 Minecraft 版本（支持多个版本，用逗号或空格分隔）
  - 使用常用版本列表（预设：1.21.11, 1.21.10, 1.21.9, 1.21, 1.20）

#### 2. 多线程并发检测
- 新增两种检测模式：
  - 顺序检测：适合少量模组，按顺序逐个检测
  - 多线程检测：适合大量模组，并发检测大幅提升速度
- 可自定义线程数（1-10），默认 5 个线程
- 使用 `ThreadPoolExecutor` 实现并发检测
- 每个模组独立检测，互不影响

#### 3. 改进的用户界面
- 更清晰的交互式菜单
- 实时显示检测进度（[当前/总数]）
- 更友好的版本输入提示
- 支持多种输入格式（逗号、空格分隔）

### 🔧 技术改进

- 添加 `concurrent.futures` 模块支持多线程
- 新增 `get_version_input()` 函数处理版本选择
- 新增 `check_mod_updates_parallel()` 函数实现并发检测
- 优化错误处理，单个模组检测失败不影响其他模组
- 改进结果输出格式

### 📊 性能提升

- 多线程模式下，检测速度提升 3-5 倍（取决于网络状况）
- 105 个模组检测完成时间从约 10 分钟缩短至 2-3 分钟
- 支持同时检测多个 Minecraft 版本，自动并行查询

### 📝 使用示例

```bash
# 运行程序
python mod_version_checker.py

# 选择版本获取方式
请选择版本获取方式:
1. 自动获取最新 Minecraft 版本
2. 手动指定 Minecraft 版本
3. 使用常用版本列表 (1.21.11, 1.21.10, 1.21.9, 1.21, 1.20)

请输入选项 (1/2/3，默认 1): 2

# 手动输入版本
请输入要检测的 Minecraft 版本，多个版本用逗号或空格分隔
例如：1.21.11, 1.21.10, 1.21.9 或 1.21.11 1.21.10 1.21.9
输入版本：1.21.11,1.21.10

# 选择检测模式
请选择检测模式:
1. 顺序检测 (适合少量模组)
2. 多线程检测 (适合大量模组，速度更快)
输入选项 (1/2，默认 2): 2

# 设置线程数
输入线程数 (默认 5，范围 1-10): 5
```

### ⚠️ 注意事项

- 多线程模式下，线程数不宜设置过大，建议 3-5 个
- 网络状况较差时，建议减少线程数或使用顺序检测
- CurseForge API 可能需要 API Key 才能稳定访问

---

## v1.0.0 - 初始版本

### 功能特性
- 支持 Forge、Fabric、NeoForge 模组格式
- 自动获取最新 Minecraft 版本
- 查询 CurseForge 和 Modrinth 平台
- 生成 CSV 检测报告
