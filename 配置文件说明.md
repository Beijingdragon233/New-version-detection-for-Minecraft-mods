# 配置文件说明

配置文件 `config.json` 用于存储程序的配置信息，包括 API 密钥和平台优先级等。

## 配置项说明

### `curseforge_api_key`
- **类型**: 字符串
- **默认值**: 空字符串 `""`
- **说明**: CurseForge API 密钥。如果不填写，CurseForge 搜索功能可能受到速率限制。
- **获取方式**: 访问 [CurseForge Console](https://console.curseforge.com/) 创建 API 密钥

### `platform_priority`
- **类型**: 字符串
- **默认值**: `"modrinth"`
- **可选值**: 
  - `"modrinth"` - 优先搜索 Modrinth 平台
  - `"curseforge"` - 优先搜索 CurseForge 平台
- **说明**: 设置搜索平台优先级。程序会优先搜索优先级高的平台，如果找到匹配版本则不再搜索另一个平台。

### `thread_count`
- **类型**: 整数
- **默认值**: `5`
- **范围**: 1-10
- **说明**: 多线程检测时的默认线程数。线程数越多检测速度越快，但可能会增加 API 请求压力。

### `timeout_seconds`
- **类型**: 整数
- **默认值**: `5`
- **范围**: 1-30
- **说明**: API 请求超时时间（秒）。如果网络较慢，可以适当增加此值。

### `log_level`
- **类型**: 字符串
- **默认值**: `"INFO"`
- **可选值**: 
  - `"DEBUG"` - 调试模式，记录所有详细信息（包括 API 请求细节）
  - `"INFO"` - 正常模式，记录关键信息
  - `"WARNING"` - 仅记录警告和错误
  - `"ERROR"` - 仅记录错误
- **说明**: 设置日志记录级别。调试问题时建议使用 `DEBUG` 级别。

## 配置示例

### 示例 1：仅使用 Modrinth（推荐）
```json
{
    "curseforge_api_key": "",
    "platform_priority": "modrinth",
    "thread_count": 5,
    "timeout_seconds": 5,
    "log_level": "INFO"
}
```

### 示例 2：使用 CurseForge API Key
```json
{
    "curseforge_api_key": "your-api-key-here",
    "platform_priority": "curseforge",
    "thread_count": 8,
    "timeout_seconds": 10,
    "log_level": "DEBUG"
}
```

### 示例 3：双平台搜索
```json
{
    "curseforge_api_key": "your-api-key-here",
    "platform_priority": "modrinth",
    "thread_count": 5,
    "timeout_seconds": 5,
    "log_level": "INFO"
}
```

## 注意事项

1. **API Key 安全**: 请勿将包含 API Key 的配置文件上传到公开代码仓库
2. **平台优先级**: 如果两个平台都有模组，程序会返回优先级高的平台结果
3. **线程数**: 不建议设置超过 10 个线程，可能会导致 API 请求被限制
4. **超时设置**: 网络环境较差时，建议增加 `timeout_seconds` 值
5. **日志级别**: 
   - 正常使用使用 `INFO` 级别
   - 调试问题时使用 `DEBUG` 级别，可查看详细的 API 请求信息
   - 日志文件保存在 `logs/` 目录下
